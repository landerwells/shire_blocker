use std::thread;
// use crate::daemon::start_daemon;

// #[test]
// fn test_daemon_start() {
//     thread::spawn(move || {
//         // Start the shire daemon process
//
//         start_daemon().expect("Failed to start daemon");
//
//
//     });
//
//     // This test is expected to fail
//     assert!(false, "This test is designed to fail");
//
//     // Launch the daemon in a separate thread
//
//
//     // Launch the bridge and pass a test message to it
// }
//
// #[test]
// fn test_install() {
//     // Call the cli with service start
// }
